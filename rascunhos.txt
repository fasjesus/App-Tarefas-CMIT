import Carousel from 'react-native-snap-carousel'; //carrossel

//carrossel 
    const data = [
        { id: 1, title: 'Botão 1', onPress: () => console.log('Botão 1 pressionado') },
        { id: 2, title: 'Botão 2', onPress: () => console.log('Botão 2 pressionado') },
        // Adicione mais botões conforme necessário
      ];
    
      const renderItem = ({ item }) => (
        <TouchableOpacity style={styles.carouselItem} onPress={item.onPress}>
          <Text>{item.title}</Text>
        </TouchableOpacity>
      ); //carrossel

 carouselItem: { //carrossel
    width: 300,
    height: 200,
    backgroundColor: '#73C5BF',
    justifyContent: 'center',
    alignItems: 'center',
    borderRadius: 10,
  },//carrossel

  <Carousel
        data={data}
        renderItem={renderItem}
        sliderWidth={300}
        itemWidth={300}
      />